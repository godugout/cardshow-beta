
// Re-export unified types
export type { Card, CardStyle, TextStyle, CardMetadata, DesignMetadata, MarketMetadata, CardTemplate } from '@/lib/types/core';

// Legacy compatibility
export type { Card as CardData } from '@/lib/types/core';
