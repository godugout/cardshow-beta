
// Re-export unified types
export type { Card, CardStyle, TextStyle, CardMetadata, DesignMetadata, MarketMetadata } from '@/lib/types/unifiedCardTypes';
export type { CardTemplate } from '@/lib/types/unifiedCardTypes';

// Legacy compatibility
export type { Card as CardData } from '@/lib/types/unifiedCardTypes';
